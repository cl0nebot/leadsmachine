<%= form_for(@callstat) do |f| %>
  <% if @callstat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@callstat.errors.count, "error") %> prohibited this callstat from being saved:</h2>

      <ul>
      <% @callstat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :machine_id %><br>
    <%= f.text_field :machine_id %>
  </div>
  <div class="field">
    <%= f.label :calls %><br>
    <%= f.text_field :calls %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
