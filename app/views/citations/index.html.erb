<p id="notice"><%= notice %></p>

<h1>Listing Citations</h1>

<table>
  <thead>
    <tr>
      <th>Citation source</th>
      <th>Property</th>
      <th>Napmatch</th>
      <th>Url</th>
      <th>Pa</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @citations.each do |citation| %>
      <tr>
        <td><%= citation.citation_source %></td>
        <td><%= citation.property %></td>
        <td><%= citation.napmatch %></td>
        <td><%= citation.url %></td>
        <td><%= citation.pa %></td>
        <td><%= link_to 'Show', citation %></td>
        <td><%= link_to 'Edit', edit_citation_path(citation) %></td>
        <td><%= link_to 'Destroy', citation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Citation', new_citation_path %>
